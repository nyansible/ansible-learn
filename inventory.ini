# Inventory file - lists all hosts Ansible can manage
# Format: hostname or IP, optionally with variables

# Special group for running on your local machine
[local]
localhost ansible_connection=local

# Example of how you'd add remote servers later:
# [webservers]
# 192.168.1.10 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/key.pem
# 192.168.1.11 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/key.pem

# [databases]
# db1.example.com ansible_user=admin
