[defaults]
# Inventory file location
inventory = inventory.ini

# Don't check host keys (useful for learning, disable in production)
host_key_checking = False

# Show task timing
callbacks_enabled = timer

# Cleaner output
stdout_callback = yaml
